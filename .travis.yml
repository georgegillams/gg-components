language: node_js

node_js:
  - 10

install:
  - npm i
  - npm run build
  - npm run transpile

script:
  - npm test
  - npm run backstopjs:test

notifications:
  email:
    on_failure: change

cache:
  npm: true
  directories:
    - node_modules


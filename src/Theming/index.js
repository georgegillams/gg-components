import { ThemeContext, ThemeProvider, THEMES } from './themeContext';
import { withTheme } from './withTheme';

export default {
  ThemeContext,
  ThemeProvider,
  THEMES,
  withTheme,
};
export { ThemeContext, ThemeProvider, THEMES, withTheme };

import React from 'react';

import { CopyButton } from './index';

export default { title: 'Copy button', component: CopyButton };

export const Default = () => <CopyButton text="This was a JS copy test." />;
